[phases.setup]
nixPkgs = ["nodejs_20", "python3", "git", "curl", "dnsutils", "iputils-ping"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"
